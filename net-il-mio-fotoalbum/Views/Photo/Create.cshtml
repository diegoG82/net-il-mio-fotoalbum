﻿@{
    Layout = "_CustomLayout";
}

@model PhotoFormModel

<h1 class="text-center">Create New Photo</h1>

<div class="container photoform mt-4 mb-4 w-50">

    <form asp-controller="Photo" asp-action="Create" enctype="multipart/form-data" method="post" class="text-center">
        @Html.AntiForgeryToken()
        <div class="mt-4 mb-4">
            <strong>Photo title:</strong>
            <input type="text" class="form-control" asp-for="Photo.Title">
            <span asp-validation-for="Photo.Title" class="text-white"></span>
        </div>

        


        <div class="mt-4 mb-4">
            <strong>URL Immagine:</strong>
            <input type="text" class="form-control" asp-for="Photo.ImageUrl">
            <span asp-validation-for="Photo.ImageUrl" class="text-danger"></span>
        </div>

        <div class="mt-4 mb-4">
            <label asp-for="ImageFormFile">Choose: </label>
            <input type="file" asp-for="ImageFormFile" />
            <span asp-validation-for="ImageFormFile" class="text-error"></span>
        </div>


        <div class="mt-4 mb-4">
            <strong>PHOTO DESCRIPTION:</strong>
            <input type="text" class="form-control" asp-for="Photo.Description">
            <span asp-validation-for="Photo.Description" class="text-white"></span>
        </div>

     

        <div class="mt-4 mb-4">
            <strong>CATEGORIES:</strong>
            <select asp-for="SelectedCategoriesId"
                    multiple="multiple"
                    size="5"
                    class="form-control mb-2 mr-sm-2 ml-sm-3 leftList"
                    asp-items="@Model.Categories"
                    data-checklist>
            </select>
        </div>

    <button type="submit" class="btn btn-success mt-4 mb-4">Update</button>
    </form>
   

</div>

